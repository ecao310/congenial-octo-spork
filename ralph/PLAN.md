Backlog 7
[x] make "Lines" dropdown render on the corner of the graph to take up less vertical space. simplify it to just have checkmarks for irmaa and 400% fpl. no explainers there needed. also call the dropdown something different like "breakpoints".
[x] Charity and municipal interest. Includes municipal interest in total income, but excludes QCD.
[x] Summary on bottom left should dynamically include the advanced inputs. Example: One year’s return: 2026 brackets and standard deduction, a married couple filing jointly, one spouse 65 or older, collecting $38,292 of Social Security per year. Plus $3750 in municipal interest and $26750 qualified charitable distributions.
[ ] Make the graph shift right when QCD increases. Text should say something like Total Income, including $x of Social Security and $y municipal interest. Excluding all qualified charitable distributions.
[x] Check against irs numbers
[ ] Text shouldn’t have meta references. For example, remove wording like “A gift anywhere near that figure is more income than step 2’s chart used to draw, so the slider runs to the statutory limit and that chart’s right edge moves out to hold it.”

Backlog 6 (21 Aug 2026)
[x] remove "next - step 2 of 2" link. remove tab links for your benefit and tax torpedo since there's only 1 thing now. 
[x] filing status should have single and MFJ as the first 2 options. then a dropdown menu reveals head of household and filing separately. actually remove the less common 2 statuses completely.
[x] remove "You are here label" on graph. x-label should say total income ($) including $z social security.
[x] remove these sentences:  where the dashed amber line crosses the curve above — that point on the curve, not the curve itself, is what the slider moves. That is the average across every dollar of it; the figure before it is the price of the next one.  joint return puts both benefits on one line...
[x] The 1411 surtax is priced everywhere and can never be anything but zero. `netInvestmentIncomeFor` (tax.ts:1488) returns `max(0, ltcg)` and nothing on the page sets `ltcg` — Backlog 5 took the capital-gains step off — so measured at $50,000, $150,000, $300,000 and $1,000,000 of ordinary income against the default benefit, `netInvestmentIncomeTax` is $0 and `totalFederalTax` equals `totalTax` to the dollar every time. The whole chapter added in Backlog 4 — `NIIT_RATE`, `NIIT_THRESHOLDS`, `niitMagi`, `niitFor`, `netInvestmentIncomeTax` and their tests — reaches no reader. Two source comments still describe it as live: `marginalRateCurve` (tax.ts:1262) explains that it plots `totalFederalTax` rather than `totalTax` because "that band is a third hump on this very axis", and `totalFederalTax` (tax.ts:1589) says "`totalTax` stays chapter 1 on its own so the close can name the surtax as its own line rather than burying it in the income tax" — and the close has six figures, none of them the surtax. README.md:79 already admits the surtax is always zero, so the repo now says both things in two places. Three ways out: move the 1411 block to `src/shelf/` beside the other code with no reader, keep it and correct both comments to say it is dormant until gains return, or give the page the one live route to net investment income that does not need the gains step back — a taxable-interest input, since `ordinaryIncome` is read as pensions, IRA withdrawals and wages and 1411(c)(5) excludes all three.
[x] The README's front door sends readers to a different application. `origin/main` is 75 commits behind `origin/dev` — 41 files, +31,097/−5,657 — and still serves `<title>Marginal Tax Rate</title>` out of a package called `growth-projector`. README.md:12 says "**Live:** https://ecao310.github.io/congenial-octo-spork/", which is main's build; the page that README actually describes is at `/preview/`, mentioned 150 lines lower under Deployment. So every reader who follows the front-door link lands on the pre-rewrite app. The dev/preview split was deliberate and merging is a real decision rather than a chore, but nothing about it obliges the README to point at the wrong build. The two moves are independent: fix line 12 to name the preview as the live page for as long as dev is the working branch, and separately decide whether 75 commits of finished, green work now belong on main. If they do, `deploy-preview.yml`'s whole reason to exist — rebuilding main's site so the production URL never changes under it — goes with them.
[x] `<meta name="description">` still advertises a section that came off the page. index.html:17 promises "the Social Security tax torpedo, capital-gains stacking, IRMAA cliffs and the senior deduction phaseout", and capital-gains stacking has not been on the page since Backlog 5 — `App.test.tsx:567` asserts its heading is absent. The `og:description` and `twitter:description` were rewritten in the same pass and name only the torpedo, so all three now disagree, and the one that is wrong is the one a search engine reads. `meta.test.ts`'s "says the same thing on both surfaces" (:61) compares og against twitter and never against `name="description"`, which is why the drift was invisible. One sentence to rewrite and one assertion to widen.
[ ] The page has no landmarks and no way past step 1. Everything renders inside `<div className="card">` (App.tsx:1535) with the `<h1>` loose in it: there is no `<main>`, no `<header>`, no `<nav>` and no skip link, so `<footer>` is the only landmark a screen-reader user can jump to on a page whose whole content is above it. Step 1 is ten controls deep before step 2's chart begins, and the step nav that would skip them is `role="toolbar"` rather than a navigation landmark. This page has already paid for a live region and for chart figcaptions, so this is the cheap remainder of that work rather than a new direction. Two decisions: whether the step nav becomes `<nav aria-label="Steps">` while keeping the arrow-key handling the toolbar role currently earns it, and whether the skip link lands on `#step-torpedo` or on `#answer`, which is what a reader returning to a link they were sent actually wants.
[ ] Every explainer heading is an `<h2>` nested inside step 2, which is also an `<h2>`. The document has one `h1`, eight `h2`s and no `h3` or `h4` anywhere: step 1, step 2, the five collapsed explainers under step 2's chart, and the close all sit at one level. So a reader navigating by heading is told that "What is the tax torpedo?", "How to mitigate the tax torpedo", "Medicare's IRMAA cliffs", the 400% poverty-line explainer and the senior-deduction explainer are peers of "The tax torpedo" rather than parts of it, and the outline says nothing about which step anything belongs to. The `<h2>` inside `<summary>` is a separate question and the answer there is probably to leave it — it makes each disclosure's accessible name the heading, which is what a reader wants. `styles.test.tsx`'s `the headings` block asserts only that no heading is gradient-painted; a levels-descend-by-one assertion is the same shape in the same file.
[ ] "Every control on this page rides in the link" is not true of two of them. The close (App.tsx:2696) tells the reader the address bar is the return, and `scenarioUrl` carries seven keys — `filing`, `ss`, `income`, `muni`, `qcd`, `senior`, `spouse` — which is every input in steps 1 and 2 and nothing else. It does not carry the Lines panel: `showIrmaaLines` and `showSubsidyLine` (App.tsx:753-754) are local state defaulting to `false`. So a reader who opens the panel, ticks both boxes, sees four IRMAA cliffs and the 400% line drawn across their own curve, and then presses Copy link, sends a chart with none of them on it — while the sentence beside the button says otherwise. Either two more flags join the query string, where `senior` and `spouse` already show what a boolean looks like there, or the sentence stops claiming every control. The first is the smaller edit and the second is the one that stays true when the next chart gets a panel.
[ ] The 2026 benefit figures do not reconcile with the COLA recorded beside them. `TAX_YEAR_PARAMS` (tax.ts:409) puts 2026's maximum at age 70 at $62,172 — $5,181/mo — against 2025's $61,296 at $5,108/mo. That is a 1.43% raise inside a block whose own `colaPercent` is 2.8 and whose `source` line reads "SSA (2.8% COLA)"; $61,296 × 1.028 is $63,012. The averages miss in the other direction: $23,712 to $24,852 is 4.8%, and both are commented as January figures. The averages are defensible on their own — SSA's COLA release quotes a pre-COLA average that has already drifted above the January one — but the maximum is a computed ceiling with no drift in it, so either that row is wrong or its comment names the wrong publication. `tax.test.ts:2777` pins $62,172 as a literal, which locks whichever it is in place, and the invariants above it (`:2727`, `:2735`) only ask that the maximum exceed the average and that the couple figure be twice the single one — neither of which this would fail. Worth re-reading all four rows against the fact sheet the `source` line names and saying in each comment which publication it came from.
[ ] The dev preview is publicly crawlable and nothing tells a crawler otherwise. `deploy-preview.yml` publishes dev to `/preview/` on the same origin as production, `index.html` carries a full og/twitter card that the preview build inherits, and there is no `robots.txt` in `public/`, no `<meta name="robots">` and no `<link rel="canonical">`. So one domain serves two near-identical indexable documents that name themselves the same thing. A `noindex` on the preview alone is not a static-file edit, because both builds are the same `index.html` — it needs either a `robots.txt` written into the preview tree by the workflow's own assemble step, which is the only place the two trees are distinguishable, or a canonical link built through `%BASE_URL%` the way the og URLs already are. The second points the preview at production, which is the wrong answer for as long as production is 75 commits stale — so this is the bullet above, seen from the crawler's side, and probably wants deciding with it.

Backlog 5
[x] add 400% FPL similar to IRMAA
[x] update visuals and structure. i like the look of ficalc. break this into multiple steps if necessary.
  [x] Ground the palette and the shape. One token layer naming every colour, corner and edge the page paints with, and the flat near-black register FI Calc actually uses in place of the slate radial gradient, the frosted-glass card, the 24px corners and the 32px drop shadow.
  [x] Bring the type down to FI Calc's scale. Its body is 15px and its headings stop around 1.4rem; this page opens with a 2.5rem hero and sets step headings at 1.75rem. Both gradient-painted headings go with it — FI Calc paints no text with a gradient, and killing them also retires the `-webkit-text-fill-color: transparent` knockout that the print stylesheet has to keep undoing.
  [x] Re-skin the controls. Sliders, segmented strips, the state menu, checkboxes and the two `<details>` disclosures still carry the old register's fills, 12px corners and 2px glow rings. FI Calc's controls are hairline-bordered, small-cornered, and ring only on focus.
  [x] Re-shape the shell. FI Calc caps content at 44rem and keeps a persistent 18rem configuration column beside it; this page runs 900px wide and puts every input in step 1, so a reader on step 3 cannot see the return being priced without scrolling back. Decide whether step 1's inputs become that column, and whether the step nav becomes a header.
  [x] Re-theme the four charts. `palette.ts` already gives the SVG the same tokens the stylesheet has, so this is the pass that spends them: grid, axes, tick labels, tooltip, reference-line labels and the heavy 0.5-opacity area fills under each curve, plus FI Calc's own chart metrics — 13px labels, 3px lines.
  [x] Re-derive the close, the explainers and the print sheet from the new ground. The close and the next-step boxes are accent-tinted panels sized for the old card; the explainers are separated by rules that were drawn for a lighter border; and the whole `@media print` block was written against the slate palette and needs re-reading against this one.
[x] remove all references to states. i may add them back later.
[x] Nothing prints on the three charts but the curve. Under `emulateMedia({ media: 'print' })` a built page renders 30 axis tick labels on screen and 0 on paper — the whole axis, both sets of tick values and the grid vanish, and what is left is a bare curve with no scale under it. The cause is in the print sheet's own chart block: it sets `height: auto !important` on `.chart-container` and on both recharts divs, and `ResponsiveContainer` re-measures on that change, gets zero, and unmounts the plot it was scaling. The `viewBox` trick the comment describes only works on an SVG that is still there. Confirmed identical on the commit before the charts pass, so it is not a regression from it — it belongs to the print-sheet bullet above, which is the next one in this list.
[x] Remove tax year selector. always 2026.
[x] Move Medicare's IRMAA cliffs. to be a tool tip. on the tax torpedo graph, have a settings button. Opens something with checkboxes for if IRMAA and 400% FPL breakpoints are rendered. default to off. 
[x] remove The curve in words.
[x] in cap gains section, make the graph the effective rate for capital gains, not marginal rate. so graph starts increasing after the 15% threshold.
[x] if married filing jointly, social security slider should have different maximum and average than single.
[x] remove the sections for capital gains and roth conversion from the page to keep it focused on tax torpedo. i may add it back later, so keep the code around. since sections are removed, also remove the links to the next sections from everywhere.
[x] tax torpedo graph axis should be total income. dynamic labels should display $ numbers for both SS and other income.


Backlog 4 (21 Aug 2026)
[x] Price the third stacking effect: the 3.8% net investment income tax. `tax.ts` has no IRC 1411 in it anywhere — no `niit`, no `1411`, no `3.8` — so `totalTax` (tax.ts:1165), both rate curves (:1122, :1240) and the closing "Federal income tax" figure all stop at chapter 1. The thresholds are $200,000 single and HOH, $250,000 joint, $125,000 separate, and they have not moved since 1411 took effect in 2013: the same frozen-threshold story as the $25,000/$34,000 bases this page is built around (`SS_BASE50_ENACTED`, tax.ts:237). The stacking is this app's own subject — an IRA distribution is *not* net investment income, but it raises MAGI, so every dollar of it pulls 3.8% more of an already-realized gain into the surtax. That is a third effect on the axis step 2 already draws, on top of the torpedo and the push out of the 0% gain band, and step 3 is where it would show hardest. Three decisions come with it: whether "Other Ordinary Income" is a distribution (excluded from NII) or taxable interest (included), whether 1411 joins the "Federal income tax" line at the close or gets its own line as a separate chapter on its own form, and whether the threshold becomes an `incomeAxisFeatures` entry (tax.ts:1020) — a single filer's $200,000 sits past `MIN_INCOME_AXIS`, so today's default chart would stop short of the one place it bites.
[x] Nothing on the page says the link is the return. d5bcf75 put all ten inputs in the query string and the README documents it, but the only place the page itself mentions the address bar is the failure case — `link-note` at App.tsx:1464, which appears when a link asked for something the page could not show. A reader who wants to send this to a spouse or an advisor has to work out on their own that copying the URL sends the scenario with it. The button belongs at the close, beside the seven figures that are the thing worth sending, with one sentence saying the link carries the return; `navigator.clipboard.writeText(window.location.href)` is the whole implementation and the decision is the fallback — clipboard access is unavailable over plain http and in older Safari, and a selectable text field is more code than the feature it backs up.
[x] The close is built to be screenshotted and the page cannot be printed. `:root` sets `color-scheme: dark` (index.css:12) over a slate radial gradient (:27), every explainer is a collapsed `<details>` and the three chart `<figure>`s are recharts SVGs sized to the screen — so Cmd-P today gives white text on white paper with two thirds of the prose missing. A `@media print` block covers the palette and the chart widths; the explainers are the open question, because print CSS cannot open a `<details>` in Chrome and it takes a `beforeprint` listener that opens them all and an `afterprint` that puts back the ones the reader had shut. Worth deciding whether a printed page is the whole walk or only the close, which is the part a reader would take to an advisor.
[x] No readout on the page is a live region. `aria-live` appears nowhere and the one `role="status"` (App.tsx:1464) is the link warning, so moving a slider announces the new slider value and nothing else: a screen-reader user never hears that the "you are here" sentence, the standing advice, the effective rate and all seven closing figures changed underneath them. The figcaptions added in 8b6c60e made the charts readable but they are static text, not announcements. The decision is how much to announce — wrapping the whole `answer-figures` list would read seven definitions on every drag, which is worse than silence — so the smallest useful unit is probably each step's own one-sentence readout, debounced, and the close left to be read on arrival.
[blocked: superseded — Backlog 5's "remove all references to states" took the selector, the footnote and the close's own "no state" clause off the page] The close says "no state" without saying which state. The Federal income tax gloss reads "no state, and no Medicare premium" (App.tsx:2974) and the effective rate directly under it is the one figure on this page a reader will take as "what I pay". Step 1 now asks for a home state and step 2 answers it in a footnote 900 lines above (App.tsx:2059) — and for the eight states that still tax a benefit in 2026 that footnote is exactly the qualification the close is missing. Naming the state and pointing back at the footnote is one clause. Pricing it is not on the table: `stateTax.ts` quotes rules rather than modelling them, and the field note under the selector promises the reader that it does.
[x] A shared link previews as a bare URL. `index.html` carries a title and a description and nothing else — no `og:title`, no `og:description`, no `og:image`, no `twitter:card` — so the link the query string was built for renders in Messages, in Slack or in a forum post as a raw `?year=2026&filing=...` string with nothing about what it shows. The favicon is still the `/vite.svg` the template shipped. Both are static-file edits with no code behind them; the only real work is the image, and a 1200x630 og:image is the one asset this repo has no way to generate in CI today.
[x] Every build trips Vite's chunk warning: 601.87 kB of JS in one chunk, 180.06 kB gzipped, and recharts is nearly all of it. The four charts are the page, so this is not a dependency to drop — but they are also all below the fold, and what a reader sees first is step 1's four inputs. A dynamic `import()` of the chart subtree would move most of that weight behind the first scroll, at the cost of a loading state this page has never had and of every jsdom test having to await it. The cheaper alternative, if that is judged not worth it: raise `build.chunkSizeWarningLimit` in vite.config.ts with a comment saying why, so the warning stops being noise every future build has to re-read.
[x] Every `vitest run` — and so every CI test step in both deploy workflows — opens with "Both esbuild and oxc options were set. oxc options will be used and esbuild options will be ignored. The following esbuild options were set: `{ jsx: 'automatic', jsxImportSource: undefined }`". Vitest 4 transforms with oxc while `@vitejs/plugin-react` 4 is still configuring esbuild's JSX. It is harmless — 592 tests pass — but it is the first line of every test log, and to anyone reading a failed CI run it is indistinguishable from a real misconfiguration. Worth finding out whether the fix is a plugin version that speaks to the oxc pipeline or an explicit override in the `test` block, and pinning it either way.
[x] Research and create more action items.

Backlog 3 (21 Aug 2026)
[x] Answer the h1 with a number: bring the Roth conversion sizer back as step 4. `conversionCeilings` (tax.ts:2064), `maxConversionUnder` (:2185) and `sizeConversion` (:2240) are complete, documented and covered by tests, and have rendered nowhere since the Strategies tab was deleted in 5fe9854. The page now asks "How Much Can You Take Out This Year?" and answers with a curve and a sentence of advice — never with a dollar figure. `ConversionSizing` already carries the whole answer: the largest conversion that fits under a chosen ceiling, the tax it costs, the average cost per dollar and the marginal rate on the far side. Give it the same shape as the other three steps: chart, then the one control that moves the reader along it (the ceiling picker), then explainers, then the close. The obvious chart is step 2's own curve with the ceiling drawn as a vertical and the conversion as a band running from "you are here" out to it.
[x] Put the total federal tax on the page instead of only in the tooltip. `point.totalTax` is rendered at App.tsx:305 and :368 and both are inside `CustomTooltip`/`LTCGTooltip`, so a reader who never hovers — and every reader on a touch screen — walks the whole page seeing marginal rates and never learns what the return actually owes. `herePoint` (App.tsx:811) and `hereGainPoint` (:816) already carry the figure at the reader's own point, and both slider readouts already say "You are here" and quote the rate there; the total belongs in the same sentence, alongside an effective rate that nothing on the page states today.
[x] Give both charts a text alternative. Each `chart-container` renders a bare recharts SVG with no `role`, no `aria-label` and no caption, so a screen reader gets nothing from the app's centrepiece, and the surrounding prose is all relative to the reader's own point rather than descriptive of the curve. `segments` (App.tsx:756) is the curve's shape as data already: a `<figure>` whose caption names the bands — "0% up to $X, 22.2% from $X to $Y, back to 12% past $Y" — says in one sentence what the picture says, and doubles as the plain-language summary of the torpedo's location that the page currently never states for any reader.
[x] Close the page with the reader's own answer. Between step 3's explainer and the disclaimer there is nothing: the reader walks three charts and leaves with no single place that says what this return costs. A closing recap — total income, federal tax, effective rate, the rate on the next dollar, the taxable share of the benefit, and the IRMAA tier this MAGI lands in — is the mirror of the scenario recap at the foot of step 1, which says what was set. This one says what came of it, and it is the thing a reader would screenshot.
[x] Make a scenario survivable and shareable by syncing it to the query string. App.tsx:532-543 holds the whole return in ten `useState` values, so a refresh loses it and there is nothing to send to a spouse or an advisor. All ten are scalars or short enums, so the encoding is trivial; the decisions are not — which wins on load when the URL names a year that is no longer in `TAX_YEARS`, whether the step the reader is on belongs in the link, and whether writing on every slider drag needs `replaceState` to keep the back button usable.
[x] Bring state treatment back as a footnote on step 2 rather than as a tab of its own. `stateTax.ts` is 256 lines of lookup table with its own tests and no reader, and the page's only acknowledgement that states exist is the IRMAA explainer's "the curve above is federal income tax only". Nine states still taxed Social Security in 2025, and a reader in one of them is being shown a curve that understates their own bill with nothing on screen saying so. A state selector in step 1 plus one conditional paragraph in step 2 is the whole feature — the data is text, not arithmetic, so it does not need a chart or a step.
[x] Decide where `projection.ts`, `sequencing.ts` and `lumpSum.ts` live. That is 1,845 lines of source and roughly 1,900 of test, all green, all imported by nothing, and all still accumulating polish bullets in Discovered Work that no reader can see. The two candidates are a step 5 that gives the horizon a home in the flow, or a holding directory that makes the working set honest about what ships. Deleting them is not on the table — the tab removal was explicitly a render-layer deletion — but leaving them undecided means future iterations keep spending passes on invisible code.
[x] Re-point the repo's front door at what the app now is. README.md still opens "# Growth Projector — See peaks and valleys of marginal tax rates", which is the framing commit d1cf5b3 just replaced on the page itself, and `package.json`'s `name` is still `growth-projector`. The README is also the only place the dev-preview deployment is documented, so it is worth keeping current rather than letting it describe a page that no longer exists.
[x] Research and create more action items.

Backlog 2 (21 Aug)
[x] Split the below backlog action into multiple.
[x] Remove the "What the tax-exempt interest costs" section. The muni slider itself stays — tax-exempt interest moves provisional income, so every chart reads it — it is the priced-out explainer block under the torpedo chart (App.tsx:1551) that goes.
[x] Remove the Medicare, Strategies, Over Time and State Taxes tabs from the site, leaving Tax Torpedo and Capital Gains. Keep the code: irmaa/projection/sequencing/lumpSum/stateTax and their 1,000-odd tests stay compiling and green, because these sections come back later. This is a render-layer deletion, not a code deletion.
[x] Re-add the IRMAA cliff vertical lines to the tax torpedo graph. `cliffsOnChart` is derived at App.tsx:808 and drawn at :1516 today, but the Medicare tab removal takes away the section that explains what a cliff is — so the lines need enough label on the torpedo chart to stand alone, or they read as unexplained red dashes.
[x] Replace the tab strip with a linear step-by-step flow. Step 1 is the SS benefit slider, step 2 the tax torpedo, step 3 capital gains. One page, sections in order, each ending in a clickable box that moves the reader to the next step. Decide whether the steps stay mounted (scroll) or swap (the tab strip's current behaviour) and keep the keyboard handling the strip has today.
[x] Give every step the same shape: chart first, then the one slider that moves the reader's position along it, then the collapsed explainer dropdowns, then the next-step box. Today the shared scenario block sits above the tab strip and the explainers sit under the chart, so this is the reordering that makes a "you are here" line below the chart legible.
[x] Draw a labelled dashed vertical at the reader's own position on each step's chart, driven by that step's slider. Same item as the "you are here" bullet already in Discovered Work — do them as one and strike both.
[x] Re-do the capital gains step as a share of income already entered: "of your $X other income, how much is long-term capital gains?" Gains stop being additive, which is what lets that step's ordinary-income slider go away and brings the LTCG slider out of the advanced disclosure. Audit every `totalTaxWithLTCG` caller — ordinary becomes X − LTCG, not X.
[x] Re-scale the chart x-axes so the features worth seeing fit on screen. MAX_INCOME is 150_000, which shows the SS torpedo and the first two IRMAA cliffs but cuts the 2025-2028 senior deduction phaseout in half for a joint filer (it runs $150k–$250k MFJ, $75k–$175k single). The domain probably has to depend on filing status and year rather than stay a constant.
[x] Raise the charitable distribution slider to the IRS annual maximum and extend the chart x-axis when a slider is set past the right edge. `qcdSliderMax` is `Math.min(qcdLimit, MAX_INCOME)` (App.tsx:653), so a joint return — whose 408(d)(8) limit is doubled, $216,000 for 2025 — is clipped to $150,000 by the chart's domain rather than by the statute.
[x] Say why this particular reader should move income up or down, keyed to where their slider sits: on the valley floor, climbing the hump, at the peak, or past it. The tooltip already computes "fill this valley at $x" / "stay under $y or over $z" for the hovered point (App.tsx:325-378); this is the same arithmetic as always-on prose keyed to the slider instead.
[x] Update title and hero text to be what the tool is useful for, instead of what the current parameters are set to.

Backlog
[x] Get IRS worksheet. Create test cases for the calculation.
[x] Make SS benefit input a slider from 0 to max yearly SS benefit for that year. Default to avg.
[x] Combine log.md and ralph.log. update instructions to use ralph.log. delete log.md.
[x] MFJ or single option selector. Default to Single.
[x] Create text section below graph explaining the tax torpedo.
[x] Create text section for strategies to mitigate tax torpedo. Consider Roths, taxable accounts, etc.
[x] Overlay total tax paid on the graph.
[x] "Other Income" text below graph is cut off.
[x] Add number for total income near graph. SS + other income. Add avg SS text near the max SS text.
[x] Remove graph for total tax paid. Instead, show total paid on mouse-over tool tip.
[x] Move and edit footer that this is not tax advice and to consult a professional.
[x] In a new section, create a new graph about Capital gains stacking. LTCG counts fully toward PI but is taxed in its own bracket. When extra ordinary income pushes SS into the tax base, it can also shove gains from the 0% bracket to 15% — stacking two effects. Marginal rates over 49% show up here. As input, create 1 slider total for non-LTCG, non-SS income. on graph, LTCG is the horizontal axis.
[x] Make What is the tax torpedo? and How to mitigate the tax torpedo explainers collapsible text. default to collapsed
[x] On mouseover on graph, add to tool tip. "Consider filling out this tax valley at $x". "Consider avoiding this tax hill by staying under $y or over $z".
[x] You're now on the dev branch on a worktree. Find a way to deploy this version without changing main.
[x] Create more action items based on this AI agent idea. Tier 1 — the reason people build this app

Tax torpedo curve. Plot effective marginal rate against income, not taxable SS against income. That's where 12% becomes 22.2% and 22% becomes 40.7%, then falls back down. The hump is the insight; the taxable-benefit chart only implies it.
Capital gains stacking. LTCG counts fully toward PI but is taxed in its own bracket. When extra ordinary income pushes SS into the tax base, it can also shove gains from the 0% bracket to 15% — stacking two effects. Marginal rates over 49% show up here.
Roth conversion sizing. Given a target ceiling (a bracket edge, an IRMAA tier, an inclusion ratio), solve for the largest conversion that stays under it. This is the action most users are actually there to take.

Tier 2 — completes the picture

Full 1040 chain: standard deduction + the 65+ additional amount + the temporary senior deduction (2025–2028, phases out at higher MAGI) → bracket application. Worth confirming current-year figures against IRS releases before you hardcode them.
IRMAA cliffs overlaid on the same x-axis. They're true cliffs, not phase-ins, and they use MAGI on a two-year lag, so they need their own income definition.
Muni-interest toggle. Flip tax-exempt interest on and off and watch taxable SS move. Best single "aha" in the app.
State treatment. Most states exempt benefits; a handful tax some portion, several with their own thresholds. A lookup table plus a note beats a wrong calculation.

Tier 3 — depth

Multi-year projection. The thresholds are frozen while COLAs aren't, so the same real income gets taxed more each year. Show 10–30 years to make that visible, with RMDs turning on at the applicable age.
Withdrawal sequencing comparison (taxable → traditional → Roth vs. proportional), scored on lifetime tax.
QCD modeling. Charitable distributions from an IRA bypass AGI entirely, so they reduce PI where an itemized deduction wouldn't.
Lump-sum election for retroactive awards — genuinely fiddly, needs prior-year data, worth deferring.
MFS-lived-together mode. Already handled in the code ($0 thresholds), but it deserves a loud UI warning since the curve looks broken otherwise.

[x] Roth conversion sizing: add `maxConversionUnder(ceiling, ...)` to tax.ts (binary search over the existing totalTax chain) plus a UI section that picks a ceiling — top of the 12% or 22% bracket, the SS 50% base, the SS 85% base, the top of the 0% LTCG bracket, or IRMAA tier 1 — and reports the largest conversion that stays under it, the resulting tax, and the marginal cost per dollar converted. This is the action most users are actually there to take.
[x] Age-65+ additional standard deduction: add an "age 65 or older" toggle (and a second "both spouses 65+" toggle for MFJ) adding $2,000 (single/HOH) or $1,600 per qualifying spouse (MFJ) on top of the base standard deduction for 2025. Widens the 0%-rate valley to the left of the torpedo.
[x] OBBBA senior deduction (tax years 2025–2028): $6,000 per filer age 65+ ($12,000 when both MFJ spouses qualify), phased out at 6% of MAGI above $75,000 single / $150,000 MFJ and fully gone at $175,000 / $250,000. That 6% phaseout is its own stealth surtax and should appear as a second hump stacked on the SS torpedo. Gate it behind the age-65 toggle and label it as expiring after 2028.
[x] Muni-interest toggle: add a tax-exempt interest slider. Tax-exempt interest counts toward provisional income (and toward IRMAA MAGI) but never toward taxable income, so it moves taxable SS without moving the ordinary tax base. Show the taxable-SS delta and the implied tax cost of each muni dollar beside the chart. Best single "aha" in the app.
[x] IRMAA cliffs overlaid on the ordinary-income chart: 2025 premiums use 2023 MAGI (AGI + tax-exempt interest) with true cliffs at $106k / $133k / $167k / $200k / $500k single (double for MFJ except the top tier) over a $185/mo base Part B premium. Draw them as ReferenceLines, put the annualized Part B + Part D surcharge in the tooltip, and state the two-year lag so the x-axis caveat is explicit.
[x] Married Filing Separately (lived with spouse) filing status: its provisional-income thresholds are $0/$0, so 85% of benefits become taxable almost immediately. Note the code has only 'single' | 'mfj' today — the idea dump's claim that MFS is "already handled" is wrong — so this needs a new FilingStatus plus a loud UI warning, since the curve otherwise looks broken.
[x] Tax-year selector: every figure is hardcoded to 2025 (Rev. Proc. 2024-40 brackets, OBBBA standard deductions, $61,296 max / $23,712 avg SS benefit). 2026 figures are already published (Rev. Proc. 2025-32: $16,100 single / $32,200 MFJ standard deduction, shifted brackets; $202.90 base Part B premium). Parameterize FILING_PARAMS, LTCG_BRACKETS and the SS benefit constants by year and default to the current year. The SS provisional-income thresholds stay frozen at $25k/$34k and $32k/$44k — that contrast IS the app's whole point, so surface it rather than hiding it.
[x] State treatment: reference table of the nine states that still taxed Social Security for 2025 (CO, CT, MN, MT, NM, RI, UT, VT, WV — WV phasing out through 2026) with each state's own exemption rule, displayed as text rather than computed. A lookup table plus a note beats a wrong calculation.
[x] Multi-year projection: the thresholds are frozen while COLAs are not, so the same real income is taxed more every year. Chart 10–30 years of taxable-SS share and effective rate under a COLA assumption slider, with RMDs switching on at the applicable age (73 / 75 under SECURE 2.0).
[x] Withdrawal sequencing comparison: taxable → traditional → Roth vs. proportional vs. bracket-filling, scored on lifetime federal tax across the projection horizon.
[x] QCD modeling: qualified charitable distributions from an IRA bypass AGI entirely, so they cut provisional income where an itemized deduction would not. Add a QCD slider capped at the annual limit and show the swing in taxable SS.
[x] Head of Household filing status — absent from FilingStatus today; shares the single filer's $25k/$34k SS thresholds but has its own brackets and standard deduction.
[x] Lump-sum election for retroactive SS awards (IRC 86(e)) — genuinely fiddly and needs prior-year income inputs; deliberately deferred until the Tier 2 items land.
[x] Page has gotten long. Split into tabs. For example, tab 1 should be just about the SS 50-85 tax torpedo.
[x] Simplify by starting with basic defaults and hiding advanced sliders in an advanced section.

[x] Replace the positional parameter lists in tax.ts with an options object. `sizeConversion` and `maxConversionUnder` now take eight positional arguments (ceiling, ordinary, ss, ltcg, filingStatus, seniors, maxConversion, muniInterest) and each new scenario input — QCDs, the tax year — adds another. Inserting `seniors` this iteration silently reassigned App.tsx's positional `MAX_CONVERSION` to it; the tests caught it, but the next insertion may not be so lucky.
[x] Research and create more action items.

Discovered Work
[ ] `playwright-core` is not in `package.json` and never was, so every `npm install` prunes it as extraneous — which is what this run's `npm install -D vite@8` did. `scripts/og-cover.mjs` then dies with a bare ERR_MODULE_NOT_FOUND before it draws anything, and so does every browser check the last four runs have relied on. `npm install --no-save playwright-core` restores it without touching either manifest and is what this run used. The choice is between saying that in the script's own header (where the reader hits the error) and admitting it to `devDependencies` — ~2 MB for a script CI never runs, against a verification step that has now silently disappeared once.
[ ] Neither deploy workflow pins a Node version. There is no `actions/setup-node` step in either, so `npm install` runs on whatever the ubuntu-latest runner ships, and Vite 8 declares `engines.node: ^20.19.0 || >=22.12.0` — satisfied today at Node 24 and unsatisfied the moment a runner rolls back or a future major raises the floor. Bullet 180 already wants those workflows touched for the v5 action majors; a `setup-node` with the version this repo actually develops on belongs in the same pass, and it would also make `npm ci` viable in place of `npm install`.
[ ] `src/build.test.ts` now runs a real production build in memory, and it has to force `NODE_ENV=production` around it because Vitest sets `NODE_ENV=test` and Vite reads that over `mode`. Unforced, the build it measures ships React's development copy behind the dev JSX transform — 318 kB of react chunk against the 140 that deploys. It restores the variable in a `finally` and the file is its own worker, so nothing else sees it, but it is still a global mutated mid-suite. If a second test file ever needs a real build, the shared version of this belongs somewhere better than a `beforeAll`.
[ ] The tree opened dirty for the sixth run running, and this time it held the run's whole bullet finished and green — the total-income axis, both halves of the marker named in dollars, 655 tests passing, PLAN.md unmarked and ralph.log unwritten. Verified and landed here. Six occurrences now, in three shapes: work finished but uncommitted (this one, and 2411bae's), work two thirds done with the suite red (the `PAGE_TAX_YEAR` run), and work one test from finished (the capital-gains removal). The pair every one of these bullets has converged on — `git status --porcelain` then `npx vitest run` as a run's first two acts — has now been written into ralph.log's closing note four times and enforced nowhere, which is the actual finding: a note the next run has to *choose* to read is not a guard. The two places it could live are the loop prompt, which every run does read, and a `prepare` script, which nothing would skip. Worth picking one rather than recording a seventh.
[ ] `drive.mjs` was in the root again on arrival — the fourth generation of the `probe.mjs`/`shot.mjs`/`verify-year.mjs` family that bullet 121 asks about, this time a torpedo-chart prober with port 4173 baked in. This run needed a browser too and worked around it differently: wrote the script to `/tmp`, copied it in as `dv-tmp.mjs` for the bare `playwright-core` import to resolve, and deleted it in the same command line, so nothing survived the run. That is the shape of the answer bullet 121 was looking for and it costs one `cp`/`rm` pair — but it wants writing down somewhere a run will find it, and the surviving `drive.mjs` wants deleting or committing as `scripts/drive.mjs` with its port in argv. Left in place this run rather than deleted, because it is the previous iteration's and deleting another run's untracked work is the thing these bullets keep asking runs not to do.
[ ] The tree opened dirty for the fifth run running, and this time the abandoned work was one test away from finished: the whole capital-gains and conversion removal — six files, ~2,500 lines deleted, PLAN.md unmarked, ralph.log unwritten — with `the margin rules` in styles.test.tsx red because the pass took `.conversion-advice` out and left the extractor's own `toBeGreaterThan(5)` guard behind it. Recovered this run. It is the case the earlier dirty-tree bullets predicted and the cheapest possible instance of it: `git status --porcelain` plus `npx vitest run` as a run's first two acts would have told this iteration in ten seconds that it was repairing rather than starting, and the repair was one number.
[ ] Six bullets in this list are now polish on sections that came off the page, and should not be picked until capital gains and the conversion sizer come back: step 3's effective-rate axis question, `segmentCurve` cutting step 3's segments on the wrong rate, step 4's missing 1411 ceiling, step 4's caption repeating step 2's, the axis label's asked-for-versus-allowed gift (its second half, the `LTCGTooltip` disagreement, went with the chart), and the shelf-gate bullet's own list. Same shape as the gate bullet the tab removal left behind: each is a real defect, none of them is visible, and every one of them returns with its section. Left unchecked rather than struck.
[ ] Step 3's curve is now the effective rate on the gain, and on this section's axis it falls rather than rises — which is not the shape the bullet asking for it predicted ("so graph starts increasing after the 15% threshold"). That shape belongs to the additive reading, where the gain stacks on a fixed income: flat at nothing while it fits under the 0% ceiling, then climbing toward 15% as more of it pokes above. Step 3 sweeps `gainsWithinIncome`, which holds total income still and moves only the split, so the gain that is charged 15% at the left edge reaches down into the 0% band as it grows and the average falls away — 15% flat then declining to 8% on a $150,000 single return, 10.2% then declining to nothing on the page as it opens. Both curves are true about the same return and they are pictures of different questions: "what does a gain cost me" against "what does taking this income as gain cost me". Deciding between them is deciding the axis, not the series, and the bullet asking to shelve this whole section may settle it first.
[ ] `segmentCurve` cuts step 3's segments on `marginalRate`, which is no longer the line the chart draws. The tooltip's hill and valley advice now names the next dollar rather than pointing at the picture ("The next dollar of gain is dearest through here"), which is honest but leaves a reader told to avoid a stretch with nothing on the curve marking where it starts or ends. Either the segments move to `effectiveRate` — where a monotone average has no hills to find, so the advice would go with them — or the two rates want drawing together, which is a second series and a legend this chart has never had.
[ ] The Lines panel covers the right half of the plot it draws on. It floats from a button set above the chart's top-right corner, and at 384x282 over an 800px-wide plot the two IRMAA lines furthest right are behind it at the moment the reader ticks the box that draws them. The count on the button ("Lines (4)") is the only feedback that reaches them until they close it. Three ways out, none obviously right: anchor the panel outside the figure in the configuration column, where the return's own controls already live; drop it below the plot rather than over it, at the cost of a layout shift on every open; or leave it and accept that a popover covers what it is about, which is what every popover does. Worth deciding before a second chart gets one.
[ ] `--fuchsia-bright` is now declared in both `:root` blocks and spent by no CSS rule. `.chart-key-subsidy strong` was the only thing painting with it and it went with the paragraph of key it coloured; `PALETTE.fuchsiaBright` is still spent, in the 400% reference line's label and the tooltip's own 36B row, but those are SVG attributes and inline styles, which is exactly the reach problem the chart-colour bullet above is about. So the token is alive, unspendable from the stylesheet, and invisible to `the print stylesheet`'s two tests, which only ask that paper answers for every screen colour and not that either ground spends what it declares. The type scale and the corners both have a "spends every step it declares" test; the colour ground has no counterpart, and this is the first token to need one.
[ ] The tree opened dirty for the fourth run running, and this time it held a task two thirds done with six tests failing in it — `PAGE_TAX_YEAR` added, the selector cut, the query-string key dropped, and `App.test.tsx` still asserting 2025 dollars in five places plus a chart test clicking a radio that no longer exists. Three earlier bullets in this list ask where a `git status --porcelain` guard belongs; this occurrence adds the case they were missing, which is that an abandoned run can leave the suite red rather than merely uncommitted. A guard that only reports "dirty" is not enough to tell finished work from broken work — the cheap pair is `git status --porcelain` *and* `npx vitest run` as the first two things a run does, so an iteration knows within a minute whether it is picking up a task or repairing one.
[ ] Three untracked Playwright driving scripts — `probe.mjs`, `shot.mjs`, `verify-year.mjs` — accumulated in the repo root, one per run that needed a browser, each hardcoding a stale preview port and last iteration's selectors. Deleted this run. They keep coming back because ralph.log's note says the script must sit in the project root for the bare `playwright-core` import to resolve, and nothing says to remove it afterwards. Worth deciding between a `.gitignore` line, a committed `scripts/drive.mjs` that reads its port and its checks from argv, or leaving it as a per-run scratch file the log tells the next run to delete.
[ ] Preview servers are left running by every run that starts one: ports 4173 through 4177 were all held on arrival this time, each serving whatever `dist/` looked like when that run built it. Harmless until a run reads the wrong port and verifies a build from three iterations ago. `pkill -f "vite preview"` before starting one is the whole fix, and it belongs next to whatever the bullet above decides about the driving script.
[ ] Every line, wash and in-plot label inside the three charts prints in its screen colour. The print sheet re-points `:root` and a CSS rule beats an SVG presentation attribute, which is how the grid, the axes and the tick values now come off the printer in paper ink — but everything App.tsx writes from `PALETTE` is out of that block's reach in the way that matters. Measured on a built page under `emulateMedia({ media: 'print' })`: `400% FPL` is `--fuchsia-bright` at 1.9:1 on white, `You are here` is `--amber` at 2.1:1, the conversion figure is `--indigo-bright` at 2.2:1 and step 3's marker is `--emerald` at 1.8:1 — the words that name every cliff, and they are the faintest thing on the sheet. A blanket CSS override cannot fix it: `hereLine` takes its colour from the slider that drives it, amber on steps 2 and 4 and emerald on step 3, and step 3's own prose says "where the dashed emerald line crosses the curve" — so one `.here-line text { fill }` would make that sentence wrong. The fix that scales is to stop writing colour into attributes at all: `stroke`/`fill` as `style={{ stroke: 'var(--rose)' }}` resolves through the same token layer both grounds already declare, and the whole chart layer re-points for free. That is a pass over `palette.ts` and every `<Area>`, `<ReferenceLine>` and `<ReferenceArea>` in App.tsx, and it needs deciding whether `palette.ts` stays the source of truth for the SVG or becomes a second name for `:root`.
[ ] Four sites on the page printed `\u2014` at the reader instead of an em dash, from 7aa0e66 until this run — fourteen commits. Fixed here, along with a `src/prose.test.ts` that walks every `.tsx` with the TypeScript AST and fails on any backslash in a JSX text node or a string-literal JSX attribute. What is not decided is whether the guard belongs in the linter instead: a rule reads at the point of the typo, where a test reads only after a run, and `no-irregular-whitespace` is the shape it would take. The test also cannot see a `{'\\u2014'}` expression child, which is a real string literal and correct — so the one escape it would miss is the one spelling that works.
[ ] The tree opened dirty for the third run running — 21 Aug 2026 made it three, and that time the uncommitted work was two thirds of the run's own bullet, which is the case where reverting would have thrown away the pass.
[ ] The tree opened dirty for the second run running, and this time the uncommitted change was a bug fix with no test and no log entry — indistinguishable, from the next iteration's side, from a half-finished edit worth reverting. The bullet below already asks where the `git status --porcelain` guard belongs; the second occurrence is the argument for answering it rather than leaving it noted.
[ ] An iteration can flip a bullet to `[x]` and exit without committing, and nothing in the repo notices. That is how this run opened: `2411bae` was HEAD and in sync with `origin/dev`, while the tree held the whole 400% FPL change — eight files, ~1,300 lines, PLAN.md already marked done, ralph.log never written. It verified green and landed as `fdd3772`, so the loss was a run rather than the work, but the next iteration had no way to tell finished work from abandoned work except by reading the diff. The cheap guard is a `git status --porcelain` check as the first thing a run does — which is now written into ralph.log's closing note rather than enforced anywhere. Worth deciding whether it belongs in the loop prompt, in a pre-commit hook, or nowhere, given the recovery took one pass.
[ ] The close never mentions the 400% cliff, and for a reader under 65 it is the biggest number on the page. The closing recap carries eight figures — total income, federal tax, the 1411 surtax, effective rate, the next dollar's rate, the taxable share of the benefit, the IRMAA tier that MAGI buys, and the room left to convert — and the IRMAA tier is there because Medicare's cliff is priced. 36B's is not priced and cannot be: what crossing it costs is the benchmark silver premium for the household's age and county. But `ptcFor` already returns the multiple of the poverty line and the headroom, and a reader who is 62 and $700 under the line is being handed a page that says nothing about the one line that matters most to them. The decision is whether a figure with no dollar amount belongs in a list of dollar amounts, or whether it is a sentence under it.
[ ] Nothing on the page asks where the reader's health coverage comes from, so the 400% line is drawn on age alone. `preMedicare` (App.tsx) counts who on the return has reached 65 and draws the line for everyone else — but an employer plan, a retiree plan or a spouse's plan each takes the credit away entirely, and for those readers the pink line is decoration. The explainer says so in its last paragraph, which is a fair fallback and not the same as not drawing it. One checkbox in step 1's advanced inputs ("we buy our own coverage on the Marketplace") would decide it, at the cost of a tenth input on a page whose first virtue is that it asks for four things.
[ ] `Scenario.householdSize` exists and the page never sets it, so every 36B line on screen is sized for one person or two. 26 CFR 1.36B-1(d) sizes the family as the taxpayer, the spouse and the dependents, and each dependent moves the cliff right by 400% of $5,500 — $22,000 of household income apiece. A joint return with two children meets the line at $128,600 rather than $84,600, which is $44,000 of income in the wrong direction: the drawn line is to the *left* of their own, so the page tells them they have lost a credit they still have. The field is plumbed through `resolveScenario` and tested; what is missing is a number input in step 1 and a decision about whether it belongs there or under Advanced inputs.
[ ] The poverty line drawn is the one for the 48 contiguous states and DC. Alaska's guidelines run about 25% higher and Hawaii's about 15%, so a reader in either meets the cliff at meaningfully more income than the chart shows. Step 1 already collects a home state — `stateTax.ts`'s selector — so the input exists; what does not is a second and third column in `FPL_YEAR_PARAMS`, which today carries one `firstPerson`/`perAdditionalPerson` pair per year. The explainer names the omission, which is the honest interim, and the fix is a table shape rather than a new question.
[ ] `FPL_YEAR_PARAMS[2026].cliff` encodes an act of Congress that had not happened when it was written. ARPA section 9661 as extended by IRA section 12001 expires for tax years beginning after 2025, so as enacted 2026 has a cliff — but an extension is exactly the kind of thing that passes in December, and if one does, this flag and `topApplicablePercentage` are the two figures to change and the whole chart follows. Worth a line in the source pointing at what to check and when, the way `IRMAA_YEAR_PARAMS` points at the CMS notice.
[ ] The state selector is the one control on the page that changes text and says nothing. Everything else now feeds the live region added this iteration, keyed to whichever control moved; the state menu is left out deliberately, because a `<select>` announces the option it lands on and the only thing left to read would be the whole footnote under step 2's chart — six or seven sentences of statute, which is the length this feature exists to avoid announcing. What that leaves is a reader who picks Colorado and is never told a paragraph appeared 400 lines below. The fix is not a longer reading but a shorter one: a single sentence naming whether that state taxes a benefit in the chosen year, pointing at the footnote for the rule. The decision is whether a reading that summarises rather than repeats belongs in a region whose whole premise so far is that it says what the page says.
[ ] Step 4 has no ceiling for the 1411 threshold, which is now the only priced line on the page a reader cannot size a conversion against. `conversionCeilings` (tax.ts) offers six, and the closest — IRMAA tier 1 — is measured with `conversionMeasureValue`'s `'magi'`, which is *Medicare's* MAGI with tax-exempt interest added back. 1411(d)'s is plain AGI, so this needs a seventh `ConversionMeasure` rather than a seventh entry in the existing table. It is the natural ceiling for the case the surtax explainer describes: a conversion that is itself outside 1411 and still drags a realized gain into it at 3.8%.
[ ] The three shelved modules still score on chapter 1 alone. `projection.ts`, `sequencing.ts` and `lumpSum.ts` call `totalTax` at nine sites between them, and none has moved to `totalFederalTax` — so a lifetime-tax comparison across a sequencing plan, or a 30-year projection of a return holding gains, understates every year in which MAGI clears the threshold. Nothing renders them, so it is not a visible defect; it is an inconsistency that will become one the moment they come off the shelf, and the gate bullet above already says not to pick shelf polish until they have a home. Worth deciding as part of that, not before.
[x] The two tooltips and the chart axis label disagree about what "total income" is, and the readouts now make it visible. `CustomTooltip` renders `point.income + ssBenefit` and `LTCGTooltip` renders `ordinaryIncome + ssBenefit`, while the axis label under step 2's chart defines the total as other income + SS + tax-exempt interest − anything given to charity — which is what the new effective-rate denominator uses. Set $10,000 of muni interest and the tooltip says $53,712 where the sentence below it says $63,712, for the same return. `CustomTooltip` already has `muniInterest` and computes `given`; `LTCGTooltip` would need both threaded to it.
[ ] The axis label under step 2's chart subtracts the *asked-for* gift rather than the allowed one — `− {qcd} given straight to charity`, where `qcdFor` caps the gift by the ordinary income there is to exclude it from. At $90,000 of income with $80,000 of it a gain and a $20,000 gift set, only $10,000 can be excluded, and the label overstates the subtraction by $10,000. Same defect as the "Charitable $30,000" summary line already logged below; the new total-income sentence gets it right, so the two now visibly disagree.
[x] The "Close the page with the reader's own answer" bullet above says the gap is "between step 3's explainer and the disclaimer". Step 4 now sits there, so the closing recap belongs at the foot of step 4 rather than step 3, and it has a fourth figure to carry: what the sized conversion does to the year's tax. The bullet's own arithmetic is unchanged.
[ ] Step 4's chart caption repeats step 2's word for word whenever the two axes match, which is the common case — a conversion usually lands inside the torpedo chart, so `conversionCurve` *is* `curve` and the two captions name the same seven bands and the same hump, about 2,000 words apart on the page. Repeating it was the deliberate call, on the ground that a figure should stand alone for a reader who lands on step 4 and never reads step 2. The alternative is a step 4 caption that names only what its own picture adds — the shaded band and the ceiling line — and falls back to the full band run only when `conversionAxisMax > axisMax`, which is when it draws stretches step 2 never did. Worth deciding once rather than re-litigating each time either caption is touched.
[ ] Four bullets in this list are now polish on code that renders nowhere, and should not be picked until `projection.ts`/`sequencing.ts`/`stateTax.ts` have a home again: the withdrawal-order QCD pricing, the nominal-dollar shortfall sum, the COLA anchoring question, and the 2026 state figures. They were all logged while the Over Time and State Taxes tabs were still rendered; commit 5fe9854 removed those tabs, so fixing any of them today changes nothing a reader can see. Left unchecked rather than struck, because each is a real defect that returns with its section.
[ ] The gate bullet above named four deferred bullets, and the shelf decision splits them three and one. The three that live in `src/shelf/` — the withdrawal-order gift pricing, the nominal-dollar shortfall sum and the COLA anchoring question — stay deferred, and `src/shelf/README.md` now says so where a reader of the code will find it. The fourth, the 2026 state figures, is live work: `stateTax.ts` renders as step 2's footnote, so Rhode Island's unpublished 2026 modification is on the page today.
[x] Fix pre-existing `as any` in vite.config.ts (eslint no-explicit-any error + tsc blindspot) by importing defineConfig from vitest/config
[x] Fix bookkeeping: mitigation-strategies task was completed and committed (4a85fce) but its checkbox was never flipped to [x]
[x] Fix failing test: commit c400999 swept in a human edit trimming the mitigation list from six to three strategies, leaving App.test.tsx asserting on removed QCD/muni text
[x] Fix `totalTaxWithLTCG` never letting the unused standard deduction offset LTCG — the LTCG band was `[ordinaryTaxable, ordinaryTaxable + ltcg]`, so when ordinary income underran the deduction the gains were taxed on the full band (single, $100k LTCG and no other income: $7,747.50 charged vs $5,385 correct; MFJ: $495 vs $0)
[x] Add the nested `B/` worktree directory to .gitignore — `git worktree list` shows a worktree at ./B inside the repo, so it appears as untracked and a future `git add -A` would commit the whole checkout
[x] Fix the IRMAA top tier being treated as exclusive for single and joint returns. `inclusiveFor` listed only `mfs`, so a MAGI of exactly $500,000 (single) or $750,000 (joint) was priced one tier low — the whole top surcharge missed. The last row of the statutory rate table at 42 U.S.C. 1395r(i)(3)(C)(i)(III) reads “At least $500,000” where every row above reads “More than”, and CMS reproduces that in all three of its tables. The inclusive top rung is not an MFS quirk.
[x] Move the "Other Ordinary Income" slider up with the filing-status/age/SS/muni inputs. The tax-exempt interest readout prices against it but is rendered above it, so the section has to say "the sliders further down"; the Roth section has the same problem with "set above" pointing at a slider two sections up.
[x] The IRMAA section prices its stat grid against the ordinary-income and LTCG sliders, which live two and three sections below it — the same ordering problem already logged for the tax-exempt interest section. Both go away with one slider move.
[x] Head of Household is now the only status missing from the segmented control, and the IRMAA table has grown a column per status — a fourth will not fit. Rework `.tier-table` to render only the selected status's thresholds (plus one comparison column) before adding HOH.
[x] The state-treatment table shows only the selected year's income test, so the 2025-vs-2026 contrast that motivates the whole year selector is invisible there — Minnesota's thresholds move $2,460 and Rhode Island's have not been published at all. Consider a second column, or a delta line, once the `.tier-table` rework lands.
[x] The multi-year projection indexes forward from the start year's own published figures, so a 2025 projection models 2026 at the user's assumed rate rather than reading Rev. Proc. 2025-32. Consider anchoring each year on the latest published `TAX_YEAR_PARAMS` entry at or below it and indexing only past that — at the cost of a kink in the curve the assumption slider does not explain.
[x] The sequencing section adds five more sliders below the chart it feeds, so the section now runs to nine inputs and the reader scrolls past the table to reach half of them. Same ordering problem already logged for tax-exempt interest and IRMAA; consider one shared "your accounts" input block above all the charts.
[x] `SequencingComparison.anyShortfall` is a `some`, but the prose it drives reads as universal ("These accounts do not last N years"). Every case found so far has all three orders running dry together — the pool they spend is the same — but a partial shortfall should be reachable at the margin where one order's earlier tax bill tips it over first. Either prove it cannot happen or branch the sentence.
[x] The QCD slider sits with the filing-status/age/SS/muni inputs but its section prices against the ordinary-income and LTCG sliders two sections below, so the prose says "the sliders further down" a third time. Third instance of the same ordering problem; one shared "your accounts" input block above all the charts fixes all three.
[x] The QCD section deliberately does not reach the multi-year projection or the withdrawal-sequencing comparison, and says so in a field note. A recurring QCD is the single most valuable thing to model over a horizon — it satisfies the RMD, skips the tax base, and shrinks the balance every later RMD is measured against — so consider threading an annual gift through `projectYears` and `compareSequencing` as a fourth strategy.
[ ] The withdrawal-order comparison now makes the same recurring gift in all three orders, but only reports the totals given — it never prices what the gift is worth to each one. The projection beside it does, by running the same horizon twice and differencing `lifetimeRealTax`; the same trick here costs three more simulations and answers the question the section is actually for: does a recurring QCD change which order wins? Bracket filling is the one to check, since emptying the IRA early and giving from it every year want the same dollars.
[x] The MFS warning quoted "$375,800 of taxable income" as a literal — the 2025 top of the separate 35% band, where the separate and single rate schedules part company. It is indexed like everything else ($384,350 for 2026), so the sentence went stale the moment the year selector landed. Now read off `filingParams(year, 'mfs').brackets`.
[x] The `.tier-table` rework that the HOH task was gated on turned out not to be needed: 42 U.S.C. 1395r(i)(3)(C) has three clauses, not one per status, and head of household shares the single column, so the IRMAA table stayed at six columns and was only relabelled. The state-treatment bullet above still says "once the `.tier-table` rework lands" — that dependency no longer exists, so the 2025-vs-2026 column there needs deciding on its own merits.
[ ] Qualifying Surviving Spouse is the one status left off the menu, and the HOH note now tells readers so. IRC 2(a) gives a widow or widower with a dependent child the joint rate schedule and joint standard deduction for the two years after the year of death — but 86(c)(1) keys its $32,000 base to "a joint return", which a surviving spouse does not file, so the SS thresholds stay at $25,000/$34,000. That pairing exists in no other status and is exactly the mismatch this app is built to show.
[x] Fix the link note's box never drawing. d5bcf75 wrote the whole `.link-note` treatment — amber border, 3px left rule, tinted ground, 13px type — by typing over the existing `.hint-bubble .field-note` rule, and kept its `.hint-bubble ` prefix. No `.link-note` has ever rendered inside a hint bubble; it is a direct child of `.card` (App.tsx:1548), so from the day it landed the note that tells a reader their link was clamped has drawn as unboxed body-size text. The same slip took `.hint-bubble .field-note { margin: 0 }` global, where a later `.field-note` rule overrode it, so the first line inside the hint bubble gained 8px it was never meant to have. Both selectors restored, and `src/styles.test.tsx` now renders the page and fails on any `.a .b` rule that matches nothing — the one selector shape that goes wrong by editing the wrong half of a copied selector, and the one whose failure is completely silent.
[x] The lump-sum section adds two more sliders (months of back pay, income during each waiting year) and prices them against the ordinary-income and LTCG sliders further up — but unlike the tax-exempt-interest/IRMAA/QCD instances of this problem, its inputs sit *with* the section that uses them. It reads better than the three logged above, which is an argument for moving those down rather than moving this one up. Worth settling one way before the shared "your accounts" block gets built.
[x] Put a "you are here" ReferenceLine on both chart tabs. The shared scenario block now sits above the tab strip, so the Tax Torpedo tab shows an "Other Ordinary Income" slider over a chart whose x-axis *is* other ordinary income, and the Capital Gains tab shows an LTCG slider over a chart whose x-axis is LTCG — in both cases the slider is a point on the curve, not the curve's input, and nothing on screen says so. A dashed vertical at the slider's value, labelled, would say it.
[x] Decide where the section-local sliders belong now that the tabs landed. The projection (5), sequencing (5) and lump-sum (2) sections still carry their own inputs below the charts those inputs feed, and the Over Time tab now stacks both of the first two — twelve sliders on one tab, half of them below the thing they move. The shared block above the strip is the obvious home for the ones that are genuinely scenario-wide (birth year, spending, account balances); the ones that are section-local (months of back pay, the fill-to ceiling) are not.
[ ] The always-on prose is now the heaviest thing above the fold. Three sliders went behind a disclosure but the Tax Year fieldset still carries three paragraphs and the Age fieldset two, all of them rendered before the reader reaches a chart. The same test that sorted the sliders — does this change the picture at page load? — sorts the prose too: the frozen-threshold paragraph is the app's thesis and stays, the senior-deduction phaseout arithmetic is a footnote and does not.
[ ] Re-check the 2026 state figures now that the calendar has reached 2026. `stateTax.ts` records Rhode Island's 2026 modification as "Not published yet" — written while 2026 was still ahead, and Rhode Island indexes a year behind but normally publishes its worksheet before filing season opens, so that line is the likeliest thing on the page to have gone stale. Minnesota's 2026 figures and West Virginia's completed phase-out want confirming against the same publications in the same pass, and 2027 is now a plausible third entry for `TAX_YEARS`.
[ ] The benefit is still grown by the COLA slider in every year, including years whose COLA the SSA has already announced — a 2025 projection at 0% holds the benefit flat through 2026 when the real January 2026 raise was 2.8%. Left alone deliberately this iteration: `colaPercent` is the reader's own assumption and the axis the section teaches, and anchoring it while the filer's other income has no published figure to anchor to would break "hold your income flat in real terms" and falsify the 0% control-case prose. Anchoring the brackets had no such cost. Worth deciding on its own merits, together with what the control case should then say.
[ ] The shortfall figure the sequencing section reports is a sum of nominal dollars, while the table above it is captioned "both figures are in {startYear} dollars" and the chart plots real ones. `totalShortfall` adds `row.shortfall` straight across the horizon, so a 20-year run reports a 2044 dollar and a 2025 dollar as the same dollar. Small at one year of shortfall, misleading at ten — deflate it the way `realTotalTax` already is, or say which dollars it is in.
[ ] The step nav marks the step the reader last *asked* for, not the one they are looking at. With all three steps mounted and the page scrolling, a reader who scrolls past step 2 with the mouse leaves the nav pointing at step 1, and `aria-current="step"` goes quietly wrong. A scroll-spy (IntersectionObserver on the three sections, cheapest at rootMargin "-40% 0px -55%") would fix it, at the cost of something jsdom cannot exercise — decide whether the nav is worth a browser test, or whether the sticky-nav question should be settled in the same pass.
[x] `plannedLtcg` is honoured by no chart. It now drives step 3's own readout, but `marginalRateCurve` takes an `ltcg` field and step 2 passes it none, and `CustomTooltip` hardcodes `ltcg: 0` into `irmaaMagi` — so a reader who says they will realize $80,000 of gains still sees a torpedo curve, a total-tax figure and a set of IRMAA cliff lines priced as if they had realized nothing. IRMAA MAGI is AGI, and AGI includes capital gains, so the cliffs are the wrong ones by the full amount of the gain. Threading `ltcg: plannedLtcg` through `curve`, `cliffs` and the tooltip is the fix; it changes the shape of the app's centrepiece chart, which is why it was not done in the same pass as the reordering.
[ ] Step 3's gains slider gets no advice of its own. `standingOn` is generic over any segmented curve and `ltcgSegments` is already computed, so the classifier costs nothing there — what stops it is the wording: every branch of `StandingNote` says "income", and step 3's axis moves the *composition* of income the reader has already entered, so "take more of it as gain" and "hold income back to a later year" are different sentences about different moves. Worth its own pass, with its own prose.
[ ] The advice under the torpedo slider cannot see the IRMAA cliffs. "Clearing $40,750 — $10,750 more — takes it to 12%" is sound about tax and silent about the $1,000-plus lump a reader might step over on the way, which is exactly the kind of thing the rest of the page is careful to price. `cliffsOnChart` is already derived beside it, so the nearest cliff between the reader and wherever the advice is pointing them is one clause — and the same clause belongs in the valley branch, where "you have $5,000 of room" can be room that ends at a cliff rather than at a bracket.
[ ] The widest chart this app can now draw is $500,000 of other income — a joint return, one spouse 65+, no benefit, the full $216,000 gift — and its first 43% is a flat run at nothing. That run is what the gift buys and it is honest, but a chart that spends nearly half its width saying "no tax yet" may be a worse teacher than one that says the same thing in a sentence. Options: cap the axis and mark the gift as off-chart the way the IRMAA cliffs already are, break the axis, or leave it. Worth deciding before another input learns to widen it.
[ ] The gains chart is drawn in amber — the same stroke and gradient as the torpedo curve on step 2 — while its own "you are here" marker and slider are emerald, and the step heading carries `.section-heading-amber`. Two curves in the same colour on one page now say the wrong thing: they are no longer the same axis rescaled, they are perpendicular sweeps of one scenario. Consider giving step 3 its own hue and letting each step's curve match the slider that walks it.
[ ] A gain now crowds out a charitable distribution: `qcdFor` caps the gift by the ordinary income there is to exclude it from, and the gain is no longer part of that. The field note under the slider says so, but nothing on screen shows the excluded amount shrinking — the summary line still reads "Charitable $30,000" when only $6,000 of it can be excluded. Worth surfacing the allowed figure beside the set one.
[ ] `App.chart.test.tsx` never pins the clock, so it renders whatever `defaultTaxYear()` the calendar gives — 2026 today — while its comments quote 2025 dollars ($85,845 for the first single cliff, $212,000 of joint MAGI). Every assertion still passes because the 2025 and 2026 shapes coincide, but the file is asserting on figures it does not name and its comments are already wrong. The other two suites both `vi.setSystemTime` to a pinned year; this one should too, and its comments re-derived from whichever year it pins.
[ ] The income axis now only ever grows: `MIN_INCOME_AXIS` holds it at the $150,000 it used to be fixed at, because that is the reader's own reach on the slider and it is what puts three IRMAA cliffs on an unmarried chart. The cost is that a filer under 65 still gets the torpedo — over by $41,000 of other income — drawn in the left quarter of the plot. Worth deciding whether a narrower chart for the returns that have nothing further out to show is worth the reach it takes away.
[ ] Silence the CI deprecation annotation on both deploy workflows: actions/checkout@v4, actions/deploy-pages@v4 and actions/upload-artifact@v4 target Node 20 and are being force-run on Node 24 — bump to the v5 majors once they're released/stable
[ ] `npx vitest run` is flaky on the full seven-file run: one App.test.tsx test at random blows the 5s default timeout, taking 17s, 96s or 296s while every other test in the same run stays under 700ms. Reproduced at HEAD with the working tree stashed (one clean run, one 202s failure), so it predates the tab-input move and is not a regression. Running `npx vitest run src/App.test.tsx` alone is clean and takes 20s every time, which points at the seven parallel forks — 168 App renders each building a full recharts tree — rather than at any one test. Candidate fixes: raise `testTimeout` in vite.config.ts, cap `maxWorkers`, or find out what the stalled test is actually waiting on. CI does not run vitest today, so this is a local-only red herring until it isn't.

[x] Parameterize the IRMAA schedule by tax year. The year selector now re-prices brackets, deductions, gain bands and the benefit, but the IRMAA section is still hardcoded to the 2025 premiums ($185/mo base Part B, cliffs at $106k/$133k/$167k/$200k/$500k) — so picking 2026 leaves that one section a year stale. 2026 is published: $202.90 base Part B premium and its own thresholds. Move PART_B_STANDARD_PREMIUM, IRMAA_TIERS and IRMAA_PREMIUM_YEAR into TAX_YEAR_PARAMS, and keep the two-year lag prose honest (2026 premiums are set by 2024 MAGI).
[ ] Nothing on the page points at the close. The step nav has four buttons and the reader's answer is a fifth landmark below all of them, reachable only by scrolling past step 4's three explainers — and step 4's section no longer ends in anything that carries a reader onward, because `nextStepBox` returns null on the last step. Either the nav gets a fifth item (which would make a summary look like a step), or the foot of step 4 gets a link to `#answer` in the shape the next-step boxes taught, or the close moves above the explainers. Worth settling alongside the query-string bullet, since a shared link that lands on the answer is the same navigation question.
[ ] The close and step 1's recap both render `FILING_STATUS_PROSE[filingStatus]`, so three `getByText` assertions that meant "the recap" had to be scoped to it this iteration and one count went from 2 to 3. That is the second time a page-wide `getByText` on shared prose has broken on an unrelated addition. The `scenarioRecap()` helper exists; there is no `closingAnswer()` equivalent outside the new describe, and no rule that says prose assertions name their section.
[ ] Step 3's sweep has no rung table. `curveStepFor` gained a `> $600,000` rung this iteration so step 2 samples at most 500 points at the widest income a link may name, but `ltcgMarginalRateCurve` is still called with a literal `step: 250` (App.tsx), so the same $1,000,000 link draws 4,000 points on step 3's chart against step 2's 500. The obvious fix — pass `curveStepFor(gainsAxisMax)` — is blocked by the gains slider's hardcoded `step={500}`: `pointAt` reads the reader's position off the nearest sample at or below it, so a slider stepping finer than the sweep quotes the rate from somewhere else.
[ ] Nothing on the page says the address bar is now the share surface. A reader who never looks at it will not know a refresh survives or that the URL carries their whole return, and the closing `#answer` card is already "the thing a reader would screenshot" — a "Copy link" button there would say both. `scenarioUrl` already builds the string; what it needs is a clipboard write and a word of feedback.
[ ] Two different links can describe the same return. `spouseIsSenior` stays set when the filing status leaves `mfj` — the checkbox is only rendered for a joint return, but the state behind it is not cleared — so `encodeScenario` writes `spouse=1` onto a single filer's link, where it changes nothing. Harmless to decode and it round-trips, but it means the address for one return depends on the order the controls were touched in.
[ ] `stateTax.ts` is no longer orphaned, so the bullet above that defers four items until `projection.ts`/`sequencing.ts`/`stateTax.ts` "have a home again" is now only about the other two — the withdrawal-order QCD pricing, the nominal-vs-real shortfall and the announced-COLA bullets still wait on where `projection.ts` and `sequencing.ts` land, but nothing in that list is blocked on state tax any more. Reword it when the projection/sequencing decision is taken rather than leaving a stale name in a gate.
[ ] The IRMAA explainer still closes with "the curve above is federal income tax only", which is now the second place on step 2 that says so — the state footnote sits above it and says it at length, names the states and cites the statute. The explainer's sentence was the page's only acknowledgement that states exist when it was written; now it is a duplicate that a reader meets after the fuller one. Either cut the clause and let the explainer stay about IRMAA, or point it at the footnote.
[ ] The state reaches step 2 and stops. Steps 3 and 4 and the closing answer are federal-only in exactly the same way and say nothing about it: step 4 in particular sizes a conversion against a federal ceiling, and in Montana or Minnesota the conversion is a state-taxable event whose cost the "average cost per dollar" figure omits entirely. A reader who named their state in step 1 has told the page something the close should probably repeat, at minimum as a caveat on the conversion's price.
[ ] The reference-line labels print in their own line's colour at 11px, which on white paper is 2–3:1 contrast: sky `#38bdf8` for the conversion ceiling, amber `#f59e0b` for "You are here" on steps 2 and 4, rose `#fb7185` for the IRMAA cliffs. They are inline SVG `fill` attributes written per chart in App.tsx (`hereLine` at :244, the cliff map at :2024), so the `@media print` block can only flatten every one of them to a single colour — and that colour is the whole tie between a label and the dashed line it names, which is what `.chart-key`'s swatch exists to teach. The fix is a print-safe colour chosen in App.tsx per line, not a CSS override: either a second palette the chart picks by media query, or accepting darker accents on screen too so one colour serves both.
