# Ralph notes — durable notes at top, per-iteration write-ups appended below

## Durable notes

- This file (ralph/ralph.log) is the single ralph memory file. It is tracked
  in git (un-ignored via `!ralph/ralph.log` in .gitignore, which otherwise
  ignores `*.log`). The old ralph/log.md was merged in here and deleted.
- Tax math: src/utils/tax.ts, 2025 params for both filing statuses via
  FILING_PARAMS (type FilingStatus = 'single' | 'mfj'). Every tax function
  takes a trailing filingStatus param defaulting to 'single'. Verified
  line-by-line against IRS Pub 915 (2025) Worksheet 1 — reference doc at
  docs/irs-pub915-worksheet1-2025.md, reference implementation + grid
  cross-checks (both statuses) in src/utils/tax.test.ts.
- MFJ 2025 params: SS thresholds $32,000 / $44,000 (line 11 = $12,000);
  single $25,000 / $34,000 ($9,000). Standard deduction $31,500 (single
  $15,750). MFJ brackets: 23,850 / 96,950 / 206,700 / 394,600 / 501,050 /
  751,600 (verified vs Tax Foundation + NTU).
- Verify with: npx tsc -b && npm run lint && npm test && npm run build.

## 2026-08-19 — Task: Get IRS worksheet. Create test cases for the calculation.

- Fetched IRS Pub 915 (tax year 2025) Worksheet 1 "Figuring Your Taxable Benefits"
  from https://www.irs.gov/publications/p915 and saved it (lines 1-19 + the
  publication's worked example) to docs/irs-pub915-worksheet1-2025.md.
- Added a line-by-line reference implementation `pub915Worksheet1()` in
  src/utils/tax.test.ts (single filer, lines 4/5/7 = 0) and a new describe
  block "IRS Pub 915 Worksheet 1 (2025)" with:
  - the publication's worked example ($5,980 benefits + $28,990 other income
    -> $2,990 taxable), verified against both the reference and
    taxableSocialSecurity();
  - boundary cases at provisional income $25,000, $25,002, $34,000, $34,001;
  - an 8x11 grid cross-check (benefits 0..60k x incomes 0..100k) asserting
    taxableSocialSecurity() === worksheet result within 1e-8, reporting all
    mismatches at once.
- Existing taxableSocialSecurity() matched the worksheet on every case — no
  production-code changes needed.
- Discovered work (fixed): vite.config.ts used `defineConfig({...} as any)`,
  which failed eslint (no-explicit-any) and hid the `test` key from tsc.
  Switched to `import { defineConfig } from 'vitest/config'` and dropped the
  cast. (First attempt via `/// <reference types="vitest/config" />` did not
  augment the type in vitest 4; the import does.)
- Verified: `npx tsc -b`, `npm run lint`, `npm test` (24 passed), `npm run build`
  all clean.

## 2026-08-19 — Task: Make SS benefit input a slider from 0 to max yearly SS benefit. Default to avg.

- Verified SSA 2025 figures via web search: max benefit claiming at age 70 is
  $5,108/mo -> $61,296/yr; average retired-worker benefit is $1,976/mo (Jan
  2025, after 2.5% COLA) -> $23,712/yr.
- Added MAX_ANNUAL_SS_BENEFIT (61,296) and AVG_ANNUAL_SS_BENEFIT (23,712)
  constants to src/utils/tax.ts with an SSA-sourced comment.
- App.tsx: replaced the number input with <input type="range"> min=0,
  max=MAX_ANNUAL_SS_BENEFIT, step=12 (keeps values on monthly-dollar
  boundaries, so both the avg default and the max are exact stops). Default
  state is now AVG_ANNUAL_SS_BENEFIT. Added a live currency readout next to
  the label and $0 / $61,296 (2025 max) endpoint labels under the track.
- index.css: range-specific overrides (the generic `input` rule's box
  padding/border looked wrong on a track): accent-color #38bdf8, transparent
  background, focus-visible outline; plus .slider-header/.slider-value/
  .slider-range-labels styles.
- App.test.tsx rewritten for the slider: role='slider', default value
  23712 + '$23,712' readout, min/max attributes, change -> value + readout
  update. Note: jest-dom toHaveValue returns a *string* for range inputs
  (only type=number coerces), so assertions compare strings.
- Verified: npx tsc -b, npm run lint, npm test (24 passed), npm run build
  all clean.

## 2026-08-19 — Task: Combine log.md and ralph.log. update instructions to use ralph.log. delete log.md.

- Merged ralph/log.md's durable notes into a "Durable notes" section at the
  top of this file; per-iteration entries follow below it.
- ralph/ralph.log was gitignored (`*.log`) while log.md was tracked, so the
  combined file would have been local-only. Added `!ralph/ralph.log` to
  .gitignore and git-added this file so the notes stay in the repo.
- Deleted ralph/log.md (git rm). Only remaining "log.md" mention in the repo
  is the task bullet wording in ralph/PLAN.md, which stays verbatim by rule.
- ralph/PROMPT.md already had the instruction change (notes go in
  ralph/ralph.log instead of log.md) sitting uncommitted in the working tree;
  committed it as part of this task.
- Docs/notes-only change — no source touched, so no build/test run needed.

## 2026-08-19 — Task: MFJ or single option selector. Default to Single.

- tax.ts restructured: STANDARD_DEDUCTION / BRACKETS / SS_BASE_* consts
  replaced by exported FILING_PARAMS record keyed by FilingStatus
  ('single' | 'mfj'). MFJ figures verified via web search (Tax Foundation,
  NTU): brackets 23,850/96,950/206,700/394,600/501,050/751,600, standard
  deduction 31,500, SS thresholds 32,000/44,000.
- taxableSocialSecurity/federalIncomeTax/totalTax/marginalRateCurve all take
  a trailing `filingStatus: FilingStatus = 'single'` param — existing single
  call sites unchanged. marginalRateCurve's param order is (ss, maxIncome,
  step, filingStatus), so App now passes step=250 explicitly.
- App.tsx: fieldset + legend "Filing Status" with a two-option segmented radio
  control (Single default, Married Filing Jointly), state threaded into
  marginalRateCurve; subtitle now says "a single filer" / "a married couple
  filing jointly" per selection. index.css: fieldset reset + .segmented /
  .segmented-option styles (hidden native radio, styled span, checked =
  sky-blue pill, focus-visible ring via input:focus-visible + span).
- Tests: pub915Worksheet1 reference generalized to take filingStatus
  (line 9 = 32,000, line 11 = 12,000 for MFJ); new MFJ describe block with
  boundary cases at provisional 32,000/44,000 + 8x11 grid cross-check; MFJ
  cases for federalIncomeTax (2,000 @ 20k; 11,828 @ 100k), totalTax, and
  marginalRateCurve (0% under 31.5k deduction; 22.2% torpedo at 40k income
  with 30k benefits, reverts to 12% at 60k). App.test.tsx: default-Single +
  switch-to-MFJ tests (role 'group'/'radio', subtitle text).
- Verified: npx tsc -b, npm run lint, npm test (36 passed), npm run build
  all clean.
- Noticed: human added two new backlog bullets mid-iteration (overlay total
  tax paid; "Other Income" x-axis label cut off) — left for next iterations.

## 2026-08-19 — Task: Create text section below graph explaining the tax torpedo.

- Added a <section class="explainer"> after .chart-container in App.tsx
  (h2 "What is the tax torpedo?" + three paragraphs): provisional-income
  mechanics (50c/85c drag-in), the 1.85x effective-rate math (12% -> 22.2%,
  22% -> 40.7%), and the 85%-of-benefits cap that ends the spike, plus a note
  that the thresholds aren't inflation-indexed.
- Thresholds in the copy are live: destructured ssBase50/ssBase85 from
  FILING_PARAMS[filingStatus], rendered with formatCurrency, so the text
  shows $25,000/$34,000 for Single and $32,000/$44,000 for MFJ and updates
  with the segmented control. Copy says so explicitly.
- Deliberately no mitigation content — "strategies to mitigate" is its own
  next backlog task.
- index.css: .explainer block (top border + 2.5rem gap, slate-muted <p>,
  brighter <strong>, 1.5rem h2).
- App.test.tsx: new test asserting the heading renders, Single thresholds
  appear, and switching to MFJ swaps them ($25,000 gone, $32,000/$44,000
  present). Note getByText matches direct text nodes only, so the assertions
  target phrases outside the <strong> tags.
- Verified: npx tsc -b, npm run lint, npm test (37 passed), npm run build all
  clean (pre-existing >500kB chunk warning only).

## 2026-08-19 — Task: Create text section for strategies to mitigate tax torpedo.

- Added a second <section class="explainer"> in App.tsx after the "What is
  the tax torpedo?" section: h2 "How to mitigate the tax torpedo", an intro
  framing everything as controlling provisional income, a <ul> of six
  strategies (Roth spending + low-income-year conversions; taxable-account
  basis spending with the LTCG/qualified-dividend caveat; delay SS to 70 while
  drawing pre-tax accounts; QCDs after 70½ — limit $108,000/person in 2025,
  verified via web search vs PG Calc/Fidelity; muni interest is added back
  into provisional income, so munis don't help; bunch income past the 85% cap
  in one year rather than straddling the spike), and a closing
  not-tax-advice caveat mentioning state tax/IRMAA dependence.
- index.css: .explainer ul/li styles (muted color, 1.25rem indent, 0.75rem
  li spacing, no trailing margin).
- App.test.tsx: new test asserting the strategies heading, the two "Spend
  from ..." <strong> labels (exact direct-text matches), and the QCD/muni
  phrases (regexes against text outside <strong>). Confirmed the new copy
  doesn't collide with the existing queryByText(/\$25,000/) absence check.
- Verified: npx tsc -b, npm run lint, npm test (38 passed), npm run build all
  clean (pre-existing >500kB chunk warning only).

## 2026-08-19 — Fix: flip forgotten checkbox for the mitigation-strategies task

- The previous iteration completed the "strategies to mitigate tax torpedo"
  task fully (commit 4a85fce: App.tsx section, index.css styles, tests, log
  entry above) but never flipped its PLAN.md checkbox from [ ] to [x] — the
  commit's PLAN.md diff only added the three new human-added backlog bullets.
- Per loop rules, fixing a previous-iteration bug is this iteration's task.
  Re-verified the committed work (npx tsc -b, npm run lint, npm test —
  38 passed, npm run build — all clean), then flipped the checkbox and noted
  the fix under Discovered Work.
- No source changes this iteration; PLAN.md/ralph.log only.

## 2026-08-19 — Task: Overlay total tax paid on the graph.

- Implemented as a synced companion panel rather than a literal dual-axis
  overlay: the dataviz skill's hard rule is "never two y-scales on one plot"
  ($ vs % axis alignment is arbitrary and invents correlation). Sanctioned
  form is two aligned charts — so the marginal-rate chart now hands its x
  ticks + "Other Income ($)" label to a second AreaChart directly below
  ("Total Federal Tax Paid", 220px), both recharts syncId="income" so the
  crosshair/tooltips track together. Top chart keeps its x-axis (tick={false},
  height 10) so the plots stay width-aligned via the shared YAxis width=70.
- tax.ts: MarginalRatePoint gained totalTax (whole dollars); marginalRateCurve
  computes it from the totalTax(income) call it was already making.
- Series color #ea580c (orange-600), picked by running the skill's
  validate_palette.js against surface #0f172a: passes CVD ΔE 26.7 / normal
  34.9 / contrast ≥3:1 with the existing sky #38bdf8. (Validator also flags
  sky itself as above the dark lightness band — pre-existing brand accent,
  left alone; the two series never share a plot.)
- Tooltip style deduped into TOOLTIP_STYLE const; tax tooltip shows
  formatCurrency. Y-axis $ compact ticks. type="linear" (tax is piecewise
  linear, not stepped).
- index.css: .chart-panel-title (slate text token, per skill: text never
  wears series color) + .chart-container--tax.
- Tests: tax.test.ts — curve totalTax matches totalTax() at sampled points,
  and is non-decreasing; App.test.tsx — panel heading renders. 41 pass.
- Verified visually: served dist via vite preview, headless Chrome screenshot.
  Gotcha for future iterations: instant screenshot catches recharts'
  left-to-right mount animation mid-reveal (series look truncated), and
  --virtual-time-budget leaves the animation clip-path rect at width=0
  (series invisible). Verified instead via --dump-dom: both stroke paths span
  the full plot width (x 80→657 @ 800px window, 601 samples), panels align
  exactly; screenshot line shape matches hand-computed totals ($10,183 at
  $65K other income; $30K axis max ≈ tax at $150K).
- Note: "Other Income ($)" label (still clipped — its own backlog task) now
  lives on the BOTTOM panel's x-axis; fix it there.
- Human added a new backlog bullet mid-iteration (capital-gains stacking
  graph, LTCG on x-axis) — committed with this iteration, left for a future
  one.
- Verified: npx tsc -b, npm run lint, npm test (41 passed), npm run build all
  clean (pre-existing >500kB chunk warning only).

## 2026-08-19 — Task: "Other Income" text below graph is cut off.

- Root cause: the tax panel's XAxis label used position 'insideBottom' with
  offset -5 — negative offset pushes the label *below* the axis box, and with
  the chart's margin.bottom=0 and default axis height 30, the text baseline
  landed at y=225 in a 220px SVG, clipping the bottom of the text.
- Fix in App.tsx: XAxis height={50} (room for ticks + label inside the axis
  box) and offset 5 (positive = up/inside). index.css: .chart-container--tax
  height 220px -> 240px so the plot area isn't squeezed by the taller axis.
- Verified via vite preview + headless Chrome. --dump-dom needs
  --virtual-time-budget=5000 or the React app hasn't rendered yet (dump was
  6KB of empty shell without it). Rendered geometry: SVG 240px tall, axis
  line y=190, tick labels y=198, label baseline y=235 with dy=0 — fully
  inside, no collision. Screenshot crop confirms label fully visible (static
  text isn't affected by the mount-animation screenshot gotcha).
- Verified: npx tsc -b, npm run lint, npm test (41 passed), npm run build all
  clean (pre-existing >500kB chunk warning only).

## 2026-08-19 — Fix: failing test from human content edit swept into c400999

- HEAD was red: App.test.tsx's "lists strategies" test failed because commit
  c400999 (the axis-label fix) also removed three of the six mitigation
  strategy bullets (delay-SS-to-70, QCDs, munis) from App.tsx. That removal
  was a clean, content-only trim not mentioned in the commit message or the
  previous log entry — a human edit sitting in the working tree that the
  previous iteration's commit swept in after its test run. Lesson for future
  iterations: diff-check what you're committing (git diff --stat vs the files
  you touched) before `git commit -a`.
- Treated the trim as intentional human curation (consistent with the new
  human backlog bullet "Remove graph for total tax paid"), so fixed the TEST
  to match the three-strategy copy: dropped the QCD/muni assertions, now
  asserts the third remaining bullet "If you can't go under it, go past it."
- Human edits landed mid-iteration in PLAN.md: un-checked ""Other Income"
  text below graph is cut off" (re-opened — presumably still clipped on
  their screen despite the c400999 fix; next iteration should re-verify at
  multiple viewport widths, not just 800px), and added two new bullets
  (remove total-tax graph in favor of tooltip; not-tax-advice footer). Note
  the footer task overlaps with the existing closing caveat paragraph in the
  mitigation section.
- Human is ALSO actively editing App.tsx (further trimming the Roth/taxable
  bullet copy + removing the section intro paragraph) — left that edit
  UNCOMMITTED in the working tree on purpose; committed only App.test.tsx +
  ralph files. Tests verified green both against HEAD's App.tsx alone (via
  stash) and with the human's edit in place (41 passed each way; the three
  asserted strings survive the trim).
- Verified: npx tsc -b, npm run lint, npm test (41 passed), npm run build all
  clean (pre-existing >500kB chunk warning only).

## 2026-08-19 — Task: "Other Income" text below graph is cut off (re-opened)

- The human re-opened this after c400999's offset/height tweak. Measured the
  actual geometry with a DOM probe (dist/measure.html: a copy of the built
  index.html plus a script that reports getBBox/getBoundingClientRect, dumped
  with `chrome --headless --dump-dom --virtual-time-budget=8000`; far more
  reliable than eyeballing screenshots, and it works around the mount-
  animation gotcha that leaves series invisible in headless shots).
- Result: c400999 did not actually fix it, it just made it marginal. The SVG
  <text> label's bbox bottom sat at y=238.0 inside a 240px-tall SVG — 2px of
  clearance at every viewport width (375/640/768/1280 all identical, since the
  label's y is derived from axis geometry, not from the font). SVGs clip at
  their viewport, so any font whose descender is deeper than the headless
  fallback's (Inter, listed first in font-family, is NOT installed on this
  box but very likely is on the human's), or a larger browser default font
  size, eats through those 2px and clips "($)" .
- Fix: stop putting the label inside the SVG at all. Dropped the XAxis
  `label`/`height={50}` and render `<p className="chart-axis-label">Other
  Income ($)</p>` as normal HTML below the chart, where block flow can never
  clip it. Centred it over the *plot area* (not the container) with
  `padding-left: 70px` + `text-align: center`: the plot starts at
  margin.left(10) + YAxis width(70) = 80 and ends at svgW - margin.right(10),
  so its centre is svgW/2 + 35, which is exactly where a 70px left pad puts
  centred text. `.chart-container--tax` height 240px -> 220px since the axis
  no longer needs the extra 20px.
- Verified with the probe at 375/640/768/1280: label text centre lands on the
  plot-area centre to the 0.1px (240.5/310.5/374.5/420.0 vs plot centres
  240.5/310.5/374.5/420.0), and the nearest overflow:hidden ancestor's bottom
  is ~1000px below the label — i.e. structurally unclippable now. Cropped
  screenshot at 900px confirms visually.
- Noted but NOT fixed (not this task, and it renders fine): the last x-axis
  tick "150K" has its bbox right edge exactly equal to the SVG width at every
  breakpoint — recharts flushes the final tick against the viewport, so it is
  ~0.5px from clipping. If it ever looks shaved, give the chart a larger
  margin.right.
- The human's uncommitted App.tsx edit (further trimming the mitigation
  bullet copy + dropping the section intro) is STILL in the working tree and
  is STILL deliberately left uncommitted, per the previous iteration. My
  App.tsx hunks are in the chart section and don't overlap it, so I committed
  by checking out HEAD's App.tsx, re-applying only my edit, committing, then
  restoring the combined file to the working tree.
- Verified: npx tsc -b, npm run lint, npm test (41 passed), npm run build all
  clean (pre-existing >500kB chunk warning only).

## 2026-08-19 — Task: Add number for total income near graph. SS + other income. Add avg SS text near the max SS text.

- Added average Social Security benefit label `<span>{formatCurrency(AVG_ANNUAL_SS_BENEFIT)} (2025 avg)</span>` to `.slider-range-labels` in `App.tsx`, sitting between `$0` and the max benefit label `$61,296 (2025 max)`.
- Added dynamic Total Income display to the chart tooltips (`Other income $X · Total income $Y` where total income = other income + SS benefit) for both the marginal rate chart and the total federal tax chart.
- Added live Total Income relationship note to the chart axis label below the graphs: `Other Income ($) · Total income = Other income + $X SS`.
- Updated `src/App.test.tsx` to assert that the average and maximum benefit labels render under the slider and that the total income text updates when the slider is moved.
- Verified: `npx tsc -b`, `npm run lint`, `npm test` (41 passed), and `npm run build` all pass cleanly.

## 2026-08-19 — Task: Remove graph for total tax paid. Instead, show total paid on mouse-over tool tip.

- Removed the second "Total Federal Tax Paid" AreaChart panel and heading from `App.tsx`.
- Restored standard x-axis ticks and formatting (`tickFormatter={formatCompact}`) on the primary Marginal Tax Rate chart.
- Added a `CustomTooltip` component to the primary AreaChart that displays:
  - Header: Other income and total income (`Other income $X · Total income $Y`)
  - Series 1: Marginal Rate (e.g. `22.2%` with sky-blue accent)
  - Series 2: Total Federal Tax (e.g. `$10,183` with orange accent)
- Removed unused `.chart-panel-title` and `.chart-container--tax` CSS rules from `src/index.css`.
- Updated `src/App.test.tsx` to verify that the separate total tax panel heading is no longer rendered.
- Verified: `npx tsc -b`, `npm run lint`, `npm test` (41 passed), and `npm run build` all pass cleanly.

## 2026-08-19 — Task: Move and edit footer that this is not tax advice and to consult a professional.

- Moved the tax disclaimer out of the "How to mitigate the tax torpedo" `<section>` into a dedicated `<footer>` element at the bottom of the card.
- Updated the footer text to: "This tool is for educational purposes only and does not constitute tax or financial advice. Please consult a qualified tax professional regarding your specific situation."
- Kept the closing strategic summary inside the mitigation section: "The right mix depends on account balances, state taxes, Medicare premium surcharges, and more. The chart above makes the goal concrete: keep provisional income out of the spike, or jump clean over it."
- Added `footer` styles to `src/index.css` (2.5rem top margin, subtle top border, muted slate text, centered).
- Added test in `src/App.test.tsx` asserting the footer text and tax professional disclaimer are rendered.
- Verified: `npx tsc -b`, `npm run lint`, `npm test` (42 passed), and `npm run build` all pass cleanly.

## 2026-08-19 — Task: On mouseover on graph, add to tool tip. "Consider filling out this tax valley at $x". "Consider avoiding this tax hill by staying under $y or over $z".

- Created a robust, general curve segmentation utility `segmentCurve` in `src/utils/tax.ts` that groups contiguous points of constant marginal rate and classifies them as `hill`, `valley`, or `flat` based on whether the rate is a local maximum or local minimum.
- Updated `CustomTooltip` and `LTCGTooltip` in `src/App.tsx` to display appropriate planning recommendations:
  - If the hovered point is inside a `hill` (e.g. the Social Security torpedo spike), it recommends: `"Consider avoiding this tax hill by staying under $y or over $z"` where $y and $z are the bounds of that hill.
  - If the hovered point is inside a `valley` (e.g. the post-torpedo drop or initial 0% zone), it recommends: `"Consider filling out this tax valley at $x"` where $x is the hovered income or LTCG.
- Added named exports for `CustomTooltip` and `LTCGTooltip` in `src/App.tsx` and wrote comprehensive unit tests in `src/App.test.tsx` to verify tooltip recommendations for both ordinary and capital gains graphs.
- Verified: `npx tsc -b`, `npm run lint`, `npm test` (62 passed), and `npm run build` all pass cleanly.




## 2026-08-19 — Task: You're now on the dev branch on a worktree. Find a way to deploy this version without changing main.

- Constraint discovered: a repo gets exactly ONE GitHub Pages site, and this
  one is `build_type: workflow` sourced from `main` (`gh api
  repos/:owner/:repo/pages`). So the usual preview patterns are all
  unavailable without touching main:
  - Switching Pages source to a `gh-pages` branch (the standard
    pr-preview-action layout) breaks main's `actions/deploy-pages` step,
    which requires build_type=workflow → would require editing main.
  - Deploying dev's artifact straight to the same site would replace
    production with dev's build on every dev push.
  - A separate preview repo works but needs a cross-repo PAT for CI, and
    adding a secret is a human web-UI action → blocked.
- Chosen approach: `.github/workflows/deploy-preview.yml`, which lives ONLY on
  dev (`on: push: branches: [dev]`). It checks out both branches, rebuilds
  main's site verbatim (`npm run build`, base `/congenial-octo-spork/`), builds
  dev with `npx vite build --base=/congenial-octo-spork/preview/`, copies main's
  dist to `site/` and dev's to `site/preview/`, and publishes the combined tree.
  Verified locally that the `--base` override rewrites every asset ref in
  index.html to `/congenial-octo-spork/preview/...`.
- Why this is safe for production: the published root is byte-identical to what
  main's own workflow produces, and if main ever fails to build the job dies
  before `upload-pages-artifact`, so nothing is published at all. deploy-pages
  is atomic — there is no window where production serves a half-built site.
- Repo setting changed headlessly (no web UI): the `github-pages` environment
  had a custom deployment-branch policy allowing only `main` and `gh-pages`, so
  a dev-branch deploy would have been rejected. Added `dev` via
  `gh api -X POST repos/:owner/:repo/environments/github-pages/deployment-branch-policies -f name=dev`.
- Known wart (accepted, not a bug): a push to main runs main's workflow, which
  publishes only main's dist, so `/preview/` 404s until dev is pushed again.
  The loop pushes dev every iteration so it self-heals; `gh run rerun` on the
  latest dev run also restores it. Also added `concurrency: group: pages` to the
  dev workflow — main's workflow has none and can't be edited, so a
  simultaneous main+dev push can still make one deployment lose the race.
- Alternative if the human would rather have the PRODUCTION url serve dev:
  that's a merge, not a config change. Left as-is deliberately — "without
  changing main" reads as "don't merge yet".
- Verified: npx tsc -b, npm run lint, npm test (62 passed), npm run build all
  clean (pre-existing >500kB chunk warning only). Post-push verification of the
  live /preview/ URL recorded below.
- Live verification after push (run 32324275743, 42s, green):
  - root https://ecao310.github.io/congenial-octo-spork/ → 200, assets under
    `/congenial-octo-spork/assets/` (main's build, unchanged).
  - preview https://ecao310.github.io/congenial-octo-spork/preview/ → 200,
    assets rewritten to `/congenial-octo-spork/preview/assets/`, and the JS
    (519,685 B), CSS and vite.svg under that path all return 200 with correct
    content types — i.e. the subpath base is right, not just present.
  - headless Chrome `--dump-dom --virtual-time-budget=8000` on the preview URL
    finds the rendered app copy, so it mounts and is not a blank shell.
- Pre-existing CI annotation, not introduced here and not actionable from the
  workflow file: "Node.js 20 is deprecated ... actions/checkout@v4,
  actions/deploy-pages@v4, actions/upload-artifact@v4 forced to run on Node 24."
  Same annotation appears on main's deploys. Backlogged.
